# super-system-header-v1
# AUTHOR: M. SZUL
# CREATED: 2026-09-05 Europe/London
# PURPOSE: Core policy contracts for the Warlock/Kali authorized security lab.

[package]
name = "warlock-kali-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
uuid = { version = "1", features = ["serde", "v4"] }

[dev-dependencies]
serde_json = "1"
